= form_for @executor do |f|
  - if @executor.errors.any?
    #error_explanation
      %h2= "#{pluralize(@executor.errors.count, "error")} prohibited this executor from being saved:"
      %ul
        - @executor.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
